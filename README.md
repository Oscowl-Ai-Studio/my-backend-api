# Workspace API
Backend API 

## How to run locally
1. Clone the repo.
2. Run `docker-compose up --build`.
3. Access at `http://localhost:8000/docs`.

## Testing
Use the included `workspace_api.postman_collection.json` file in Postman.
